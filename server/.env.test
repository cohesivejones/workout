# Test Database Configuration (for local Docker setup)
PGHOST=localhost
PGUSER=test_user
PGPASSWORD=test_password
PGDATABASE=workout_test
PGPORT=5433

# Server Configuration
PORT=5001
CORS_ORIGIN=http://localhost:5173

# Authentication
JWT_SECRET=test_jwt_secret_key_for_e2e_tests

# OpenAI (not needed for e2e tests)
OPENAI_API_KEY=test_key
