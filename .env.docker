# Docker Local Testing Environment
# Copy this to .env for local Docker testing

# Server Configuration
PORT=3000

# Database Configuration (embedded PostgreSQL in container)
PGUSER=postgres
PGPASSWORD=
PGDATABASE=workout_production

# Authentication (REQUIRED)
JWT_SECRET=test-jwt-secret-change-in-real-production

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# OpenAI API (optional - leave empty if you don't have a key)
OPENAI_API_KEY=

# Environment - use development for local testing to disable HTTPS redirect
NODE_ENV=development
