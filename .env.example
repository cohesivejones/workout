# ===================================
# LOCAL DEVELOPMENT (npm run dev)
# ===================================

# Frontend development server
PORT=3000

# API URL for frontend proxy (points to backend dev server)
VITE_API_URL=http://localhost:5001

# Database Configuration (connects to local Docker PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=workout_dev

# Authentication
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production

# CORS Configuration (allow frontend dev server)
CORS_ORIGIN=http://localhost:3000

# OpenAI API (optional - for workout generation and diagnostics)
OPENAI_API_KEY=your-openai-api-key-here

# Environment
NODE_ENV=development

# ===================================
# DOCKER PRODUCTION (docker-compose.prod.yml)
# ===================================
# Create a .env file with these values for Docker deployment:

# Database Configuration (postgres is the container name in Docker network)
# PGHOST=postgres
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=<strong-password-here>
# PGDATABASE=workout_production

# Port mapping (external:internal)
# APP_PORT=3000
# POSTGRES_PORT=5432

# Authentication (REQUIRED - generate a strong secret)
# JWT_SECRET=<generate-a-strong-random-secret>

# CORS Configuration (your NAS domain/IP)
# CORS_ORIGIN=https://workout.yourdomain.com,http://192.168.1.100:3000

# OpenAI API (optional)
# OPENAI_API_KEY=sk-...

# ===================================
# HEROKU CONFIGURATION (if still using)
# ===================================
# Heroku sets these automatically:
# DATABASE_URL=postgresql://...
# PORT=<dynamic>
# NODE_ENV=production
